//Write an expression that calculates trapezoid's area by given sides a and b and height h.

var a = 23,
    b = 12.1,
    h = 12, //No idea if such trapezoid could even exist but...
    area = ((a + b) / 2) * h;

console.log("The area of the trapezoid is " + area);