using System;

class ExplainDifferenceBetweenCSharpAndDotNet
{
    static void Main()
    {
        Console.WriteLine("As a short explanation I would say that C# is a language while .NET is an application framework that consists of standart libraries. The .NET libraries can run on the CLR and thus any language which can run on the CLR can also use the .NET libraries. C# is just one of those languages.");
    }
}

