//Declare an integer variable and assign it with the value 254 in hexidecimal format. Use Windows Calculator to find its hexidecimal representation.

using System;

class AssignHexidecimalValue
{
    static void Main()
    {
        int hexidecimalInteger;
        hexidecimalInteger = 0xFE;
        Console.WriteLine(hexidecimalInteger);
    }
}